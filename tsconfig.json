{
	"extends": "@apify/tsconfig",
	"compilerOptions": {
		"allowJs": true,
		"lib": ["ESNext", "DOM"],
		"outDir": "dist",
		// Unfortunately have to enable this while we stick to ow
		"skipLibCheck": true
	},
	"include": [
		"./src/**/*"
	]
}
